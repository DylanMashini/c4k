export default function Overview() {
  return (
    <section className="section" id="overview">
      <div className="sec-head">
        <div className="sec-num">§ 00 — Overview</div>
        <h2>Overview</h2>
        <p>
          This RC car is built from four 3D-printed parts plus the electronics from the standard C4K
          Micro:Bit kit. You&apos;ll print the parts, snap them together, wire up the servos and battery,
          and clip on a shell you design yourself.
        </p>
        <p style={{ marginTop: 12 }}>
          The full build takes about an hour once the parts are printed. The guide is broken into numbered
          steps — follow them in order.
        </p>
      </div>
    </section>
  );
}
